name: Run testflows tests

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - "*"

permissions:
  contents: read

env:
  GRAFANA_VERSION: ${{ '12.4.0-20977568970' }}
  testflows_logs: |
    ./testflows_logs/*
  selenium_logs: |
    ./selenium_logs/*

jobs:
  tests:
    # runs-on: [ self-hosted, on-demand, type-cpx41, image-x86-system-ubuntu-22.04 ]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write

    timeout-minutes: 60

    strategy:
      fail-fast: false
      matrix:
        include:
          - group: window-functions
            suite: "window_functions"
          - group: sql-editor
            suite: "sql_editor"
          - group: data-source-setup-connections
            suite: "data_source_setup_connections"
          - group: data-source-setup-defaults
            suite: "data_source_setup_defaults"
          - group: e2e
            suite: "e2e"
          - group: query-options
            suite: "query_options"
          - group: functions
            suite: "functions"
          - group: macros
            suite: "macros"
          - group: adhoc-macro
            suite: "adhoc_macro"
          - group: unified-alerts
            suite: "unified_alerts"
          - group: template-variables-editor
            suite: "template_variables_editor"
          - group: legacy-alerts
            suite: "legacy_alerts"
          - group: conditional-test
            suite: "conditional_test"
          - group: search-filter
            suite: "search_filter"
          - group: flamegraph-and-tracing
            suite: "flamegraph_and_tracing"
          - group: limited-access
            suite: "limited_access"
          - group: worldmap-and-table-format
            suite: "worldmap_and_table_format"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup
        run: tests/testflows/infra/setup.sh

      - name: Cache pip packages
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('tests/testflows/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Run Altinity Grafana Plugin tests (${{ matrix.group }})
        run: cd ./tests/testflows/ &&
          python3
          -u ./regression.py --before=0.1 --after=0.1
          --suite ${{ matrix.suite }}
          --log raw.log
          -o short

      - name: Create reports
        if: always()
        run: tests/testflows/infra/create-report.sh

      - name: Upload raw coverage data
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: raw-coverage-${{ matrix.group }}
          path: |
            go_coverage/raw/
            tests/testflows/coverage/raw/
          if-no-files-found: ignore

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: testflows-logs-${{ matrix.group }}
          path: ${{ env.testflows_logs}}

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: selenium-logs-${{ matrix.group }}
          path: ${{ env.selenium_logs}}

  coverage:
    needs: tests
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Download all raw coverage artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: raw-coverage-*
          path: raw-coverage

      - name: Merge and generate backend coverage
        run: |
          mkdir -p go_coverage/raw
          for dir in raw-coverage/*/go_coverage/raw/; do
            cp -r "$dir"* go_coverage/raw/ 2>/dev/null || true
          done
          if ls go_coverage/raw/cov* 1>/dev/null 2>&1; then
            go tool covdata textfmt -i=go_coverage/raw -o=go_coverage/coverage.txt
            go tool cover -html=go_coverage/coverage.txt -o=go_coverage/coverage.html
          else
            echo "No backend coverage data found"
          fi

      - name: Normalize frontend coverage paths
        run: |
          for f in raw-coverage/*/tests/testflows/coverage/raw/*.json; do
            [ -f "$f" ] || continue
            jq '
              with_entries(
                .key |= sub("^/grafana-clickhouse/"; "") |
                .value.path |= sub("^/grafana-clickhouse/"; "")
              )
            ' "$f" > "$f.tmp" && mv "$f.tmp" "$f"
          done

      - name: Merge and generate frontend coverage
        run: |
          mkdir -p .nyc_output
          for dir in raw-coverage/*/tests/testflows/coverage/raw/; do
            cp -r "$dir"* .nyc_output/ 2>/dev/null || true
          done
          if ls .nyc_output/* 1>/dev/null 2>&1; then
            if [ -f coverage/coverage-final.json ]; then
              cp coverage/coverage-final.json .nyc_output/
            fi
            npx nyc report --temp-dir .nyc_output --reporter html --reporter=lcov --report-dir=./tests/testflows/coverage
          else
            echo "No frontend coverage data found"
          fi

      - name: Verify frontend coverage
        run: |
          if [ ! -s tests/testflows/coverage/lcov.info ]; then
            echo "::error::lcov.info is empty or missing — frontend coverage was not generated"
            exit 1
          fi
          echo "Frontend coverage generated successfully:"
          grep '^SF:' tests/testflows/coverage/lcov.info | head
          grep -c '^SF:src/' tests/testflows/coverage/lcov.info || {
            echo "::error::No SF:src/ entries found in lcov.info"
            exit 1
          }

      - name: Report coverage to Coveralls
        uses: coverallsapp/github-action@v2
        with:
          files: tests/testflows/coverage/lcov.info go_coverage/coverage.txt

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: code-coverage
          path: |
            go_coverage/coverage.html
            go_coverage/coverage.txt
            tests/testflows/coverage/
          if-no-files-found: ignore
