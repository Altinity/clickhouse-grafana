# Аппрув-лист: интерполяция переменных — сейчас vs после PR #906

**Что утверждаем:** принятие PR #906 (fix issue #905) + стандарт поведения STD-INTERP-1.
**Ветка:** `fix/interpolation-contract-905` (master + cherry-pick #906 + контракт-тест, 8 коммитов).
**Полный стандарт:** `2026-07-05-interpolation-standard.md` · **Контракт:** `interpolation-contract.test.ts` (38 кейсов, зелёные).
**Дата:** 2026-07-05 · **Автор:** Roman M

---

## Таблица классов поведения

Все значения измерены живым кодом (не выведены из документации). «falsy-конфиг» = `multi`/`includeAll` оба ∈ {false, undefined, null}.

| # | Класс | Пример SQL | Какие переменные | Сейчас (3.4.x / master) | После #906 (ветка) | Вердикт |
|---|---|---|---|---|---|---|
| 1 | Конкатенация / строковые литералы | `FROM $db.$table` · `LIKE 'pre$v%'` | все | `raw` | `raw` | ✅ без изменений (#797, #827) |
| 2 | `IN()/IN[]/NOT IN/tuple()` + скаляр | `WHERE x IN ($v)` | все | `'val'` + экранирование | `'val'` + экранирование | ✅ без изменений (#847) |
| 3 | `IN(...)` + массив | `WHERE x IN ($multi)` | мультиселект | `'a','b'` | `'a','b'` | ✅ без изменений (#838) |
| 4 | Массив вне IN | `hasAny($tags, col)` | мультиселект | `['a', 'b']` | `['a', 'b']` | ✅ без изменений (#829) |
| 5 | Repeated-панели (value ≠ current) | repeat by variable | все | `'val'` (квотит и числа, без экранирования) | то же самое | ✅ без изменений (#712); известные дефекты L-4/C4 одинаковы в обеих версиях |
| 6 | Плоская позиция, `multi:false, includeAll:false` | `WHERE db = '$db'` | query/custom, мультиселект выкл | `raw` | `raw` | ✅ без изменений |
| 7 | Плоская позиция, truthy-конфиг | `WHERE x = $v` при multi/All вкл | мультиселект, groupby | `'val'` + экранирование | `'val'` + экранирование | ✅ без изменений |
| 8 | Числа (без экзотики класса 10) | `WHERE port = $port` | все | `raw` | `raw` | ✅ без изменений |
| 9 | **Плоская позиция, нечисловая строка, falsy-конфиг ≠ `false/false`** | `FROM $var` · `WHERE $cond` · `WHERE x = $var` | **constant, textbox, interval; query/custom из старых JSON (`null`)** | `'val'` — **это баг #905**: `FROM 'db.t'` ломает идентификаторы и SQL-фрагменты | `raw` — идентификаторы и фрагменты работают; голое `= $var` требует авторских кавычек | 🔶 **ЦЕЛЕВОЕ ИЗМЕНЕНИЕ**: чинит #905, BREAKING для голых value-позиций → миграция `= '$var'`; отдельно предупредить когорту #809 |
| 10 | Плоская позиция, числовая строка + первая нечисловая опция в `options`, falsy-конфиг | `WHERE x = $v` ($v='123') | query/custom из старых JSON со смешанными options | `'123'` (escape читал options) | `123` (guard срабатывает раньше, options не читаются) | 🔶 BREAKING-грань (сценарий №15, найден полным перебором); риск type mismatch на String-колонках |
| 11 | Значение `null` при falsy-конфиге | пустая переменная | constant/textbox | **крэш `TypeError`** в `clickhouseEscape` (измерено) | `raw`, без крэша | ✅ **бонус-фикс** скрытого краша |
| 12 | Формат-спецификаторы | `${var:raw}`, `${var:sqlstring}` | все | обход плагина | обход плагина | ✅ без изменений |
| 13 | Идентификаторная позиция + truthy/repeated скаляр | `FROM $v` · `JOIN $v` · `RENAME … TO $v` | мультиселект/All; repeated-панели | `FROM 'tbl'` — **всегда невалидный SQL** | `FROM tbl` — работает | ✅ **новое правило R-13** (поверх #906; может только чинить: квотированный идентификатор не бывает валидным ClickHouse) |

**Сводка: 10 классов без изменений · 1 целевое изменение (класс 9 — сам фикс #905) · 1 экзотическая грань (класс 10) · 2 бонус-фикса (классы 11, 13).**

Гарантия односторонности: новое условие — надмножество старого, поэтому изменения возможны только в направлении `'quoted'` → `raw`; ни один класс не может «начать квотиться» (формально доказано + запинено тестом эквивалентности A-EQ).

---

## Что именно просим утвердить

1. **Поведение** — классы 9–11 и 13 из таблицы (фикс #905 ценой миграции голых `= $var` → `= '$var'` + правило R-13 для идентификаторных позиций; релиз как **3.5.0** с BREAKING-записью, migration note включает когорту #809).
2. **Источник правды** — таблица стандарта + контракт-тест (38 кейсов) объявляются нормативными; изменения поведения только парным PR «стандарт + тест» (правила M-1…M-5 стандарта).
3. **Фаза 2** (отдельным PR, поведение под защитой контракта): модуль `variables/`, экранирование в repeated-пути (L-4) и внутри литералов (L-1), нормализация `current` (L-2) — запускать сразу после мержа или отложить.

## Чем защищено от регрессий

- контракт-тест: 38 кейсов, каждая клетка таблицы запинена; смена любой клетки = красный CI + видимый дифф;
- ловушка эквивалентности `false ≡ undefined ≡ null` — возврат строгого сравнения невозможен незаметно;
- e2e-дашборд `/d/issue905-single-value` — живая Grafana, реальные рантайм-формы переменных;
- авторство PR #906 (oplehto, повторный внешний контрибьютор) сохранено нативно через cherry-pick.

| Роль | Имя | Решение | Дата |
|---|---|---|---|
| Автор | Roman M | предложено | 2026-07-05 |
| **Техлид** | ____________ | ☐ утверждаю ☐ с правками: ______ | ________ |
